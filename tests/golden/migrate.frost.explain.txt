examples/migrate.frost
======================
This script runs psql, reads 1 file (runtime), writes 1 file (runtime), deletes 1 file, sets 1 environment variable (PGDATABASE), reads 1 secret (db password), and lets a secret leave the process in 1 place. It can exit with status 1, 2.

Runs these programs:
  psql  — line 43  (given input)

Reads these files:
  (path built at runtime)  — line 18

Writes these files:
  (path built at runtime)  — line 35

Deletes these files:
  (path built at runtime)  — line 38

Sets these environment variables:
  PGDATABASE  — line 29

Reads these secrets:
  db password  — line 23  (from the keystore)

Lets a secret leave the process:
  on the standard input of psql  — line 43

Cleans up on exit:
  ensure block  — line 37

Can exit with:
  status 1
  status 2

Note: 3 name(s) are built at runtime and cannot be checked ahead of time.

Verdict: clean
